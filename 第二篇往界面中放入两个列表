#试图往界面中丢入两个列表信息用以显示

#从tkinter中导入所有方法
from tkinter import *

#创建窗口对象的背景色？
root = Tk()

#先创建两个列表
li = ['C','python','php','html','SQL','java']
movie = ['CSS','jQuery','Bootstrap']

#然后创建两个列表组件
listb = Listbox(root)
listb2 = Listbox(root)

#将第一个列表中的数据，从前往后丢进第一个列表组件
#丢进去的时候，最后一个数据将排在列表组件中的第一位
for item in li:
    listb.insert(0,item)

#将第二个列表中的数据，从前往后丢进第二个列表组件
#丢进去的时候，最后一个数据将排在列表组件中的第一位
for item in movie:
    listb2.insert(0,item)

#分别将两个已经装有数据的列表组件丢进主窗口中，以在主界面上显示
listb.pack()
listb2.pack()

#进入消息循环
root.mainloop()
